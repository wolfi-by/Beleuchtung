enum MyTypeLight{
dark = 0,
light = 1
};

 struct Color{
	byte r;
	byte g;
	byte b;
}RGB_t;

/*
static void test(void)
{
  Enum functionType
  {
    readFunction, writeFunction
  };
  typedef struct
  {
    uint8_t adress;
    uint8_t functionType;
    uint8_t data[18];
  } TelegramData_t;

  TelegramData_t buffer;
//zugriff erfolgt so:
  buffer.adress = 1;
  buffer.functionType = readFunction;
  buffer.data[0] = 4;
}*/


